color_blue = #3d6cb0;

.title{
       
        display flex;
        align-items center;
        margin-bottom 0.8em;
        
        .icon{
            width 5em;
            flex-shrink 0.1;
            margin-right 0.8em;
           
        }
        
        .title__text{
            color color_blue;
            text-transform  uppercase;
            font-size 2.8em; 
            font-family: MPBold;
            margin 0;
            margin-right 0.2em;
        }
        
        .title__line{
            border solid color_blue 2px;
            width 70%;
            flex-shrink 50;
        }
    }